type Query
type Mutation
type Subscription

scalar DateTime

extend type Mutation {
    testMutation: String
}

extend type Query {
    version: String!
    header: String!
}

extend type Subscription {
    countdown(from: Int!): Int!
}