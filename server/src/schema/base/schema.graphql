type Query
type Mutation
type Subscription

scalar DateTime
scalar File

directive @deprecated(reason: String) on FIELD_DEFINITION | ENUM_VALUE

extend type Query {
	version: String! @auth(roles: [ADMIN])

	header: String!
}
